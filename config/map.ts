const defaultProvider = {
  url: 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',
  attribution: '&copy; OpenStreetMap contributors',
  maxZoom: 19,
  tileSize: 256,
  zoomOffset: 0,
};

export const mapTileConfig = defaultProvider;
